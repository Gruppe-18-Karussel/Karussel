---
const { title, img, featured, content } = Astro.props;  
---

<article>
  <h2>{title}</h2>
  <img src={img} alt={title} />
  {featured && <p>Featured Artist</p>}
  <p>{content}</p> {/* Indsætter tekst-indholdet korrekt */}
</article>
